# Para compartir con otra persona, usa una BD en la nube (PostgreSQL) y pon la URL aquí.
# Ejemplos: Supabase, Neon, Railway, Render Postgres.
DATABASE_URL=postgresql+psycopg://usuario:password@host:5432/dbname

# Frontend local (Vite) por defecto
CORS_ORIGINS=http://localhost:5173
